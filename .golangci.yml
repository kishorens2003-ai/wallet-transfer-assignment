linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - gofmt
    - goimports

linters-settings:
  goimports:
    local-prefixes: wallet-transfer

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
